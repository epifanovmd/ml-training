"""mlkit — ядро конвейера обучения детекторов.

Ядро не знает предметных областей: оно оперирует «проектами»
(`projects/<имя>/project.yaml`) и стадиями обучения — сборка датасета,
обучение, проверка, экспорт в мобильные форматы.

На вход нужен готовый размеченный датасет — каталог в `datasets/<проект>/`.
"""

__version__ = "1.1.0"

STAGES = ("dataset", "train", "eval", "export")
