# Стадии 1–2 — сборка датасета и обучение (окружение .venv, Python 3.10–3.13).
#
# Версии закреплены намеренно: обновление ultralytics меняет и обучение,
# и формат экспорта, а пара torch/torchvision должна совпадать по минорной
# версии. Поднимать — осознанно и с перепроверкой метрик.
-r base.txt
ultralytics==8.4.116
torch==2.13.0
torchvision==0.28.0
tqdm>=4.66
